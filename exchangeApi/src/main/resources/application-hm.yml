spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/changeapp_hm
    username: changeapp
    password: changeapp123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

logging:
  level:
    com.exchange: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: WARN 